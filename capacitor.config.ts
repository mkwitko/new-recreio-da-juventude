import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'recreio-ju-new-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
