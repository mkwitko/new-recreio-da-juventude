import { AuthorizationClass } from './authorization-class';

describe('AuthorizationClass', () => {
  it('should create an instance', () => {
    expect(new AuthorizationClass()).toBeTruthy();
  });
});
