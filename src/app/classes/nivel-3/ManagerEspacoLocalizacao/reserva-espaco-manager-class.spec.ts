import { ReservaEspacoManagerClass } from './reserva-espaco-manager-class';

describe('ReservaEspacoManagerClass', () => {
  it('should create an instance', () => {
    expect(new ReservaEspacoManagerClass()).toBeTruthy();
  });
});
